# Dependencies & build output — regenerated by CI (composer install + npm run build)
/node_modules/
/vendor/
/build/

# Packaged ZIPs & logs
*.zip
*.log

# Internal-only docs kept out of the PUBLIC repo (monetization strategy,
# reviewer correspondence). Remove these lines if you want them public.
ROADMAP.md
SUBMISSION-NOTE.md

# Local caches / OS cruft
.phpunit.result.cache
.DS_Store
Thumbs.db
*.swp

